package dungeoncrawler.game.entities;

public class Player extends Entity{
    private static final long serialVersionUID = 1L;
    //this class is updating data at all times, that's why I need to give it a UID
    //To be honest, I used that mainly because many people use that, so I think why not.
    // 1L is the default number. the UID is usually required if you are implementing interface.

    private int hp;
    private

}
